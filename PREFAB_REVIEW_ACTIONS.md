# Rev A 投产前复核:优化点与修改点全表

复核日期 2026-08-31 · 工具 KiCad 10.0.6 + ngspice-42 · 发现 19 项 · 撤回误报 2 项

所有结论用独立安装的工具与厂商公开数据实测得出,不依赖 `tools/` 自带的审计脚本
—— 那套脚本既生成发布件又给发布件盖章,是个闭环。

网页版(分组更清楚):https://claude.ai/code/artifact/18295241-2712-4f6e-93bc-d84d430c0df3

## 结论

| 项 | 结果 |
|---|---|
| PCB DRC | 0 违规 / 0 未连接 |
| 网表 vs `design_data.py` | 309 焊盘,0 不符 |
| Gerber 复现 | 11/11 层逐字节一致 |
| **下单前必改** | **1 项(仅改 BOM)** |
| 需外部确认 | 4 项 |

铜箔、网表、Gerber 三者互相咬合且可复现,这一层可信。挡住下单的是一颗状态灯的限流电阻。

---

## A · 下单前必须改(阻塞下单,仅改 BOM)

### A1 D5 状态灯电流只有额定值的 2%–10%,可能完全不亮

`KT-0603G` 是 525nm 翠绿 LED,**Vf = 3.1 V @ 5 mA**(LCSC C12624)。
R9 = 1 kΩ 由 GPIO14 的 3.3 V 驱动,电阻上只剩约 0.2 V 裕量。

| GPIO | LED 拐点 | R9 | 电流 | 相对亮度 |
|---|---|---|---|---|
| 3.3 V | 2.80 V | 1 k | 0.481 mA | 41 mcd |
| 3.3 V | 2.90 V | 1 k | 0.385 mA | 33 mcd ← 设计值 |
| 3.3 V | 3.10 V | 1 k | 0.192 mA | 17 mcd |
| 3.2 V | 3.10 V | 1 k | 0.096 mA | 8 mcd ← 基本看不见 |

额定 430 mcd @ 5 mA。亮度在 Vf 分布上有 4:1 漂移,Vf 偏高的批次就是不亮。

**建议:R9 由 1 kΩ (C21190) 改成 100 Ω**,电流 1.4–2.1 mA,永远能亮。
选它是因为不换 LED、不用重新验极性 —— D5 极性上已经花过功夫,不该再动。
替代方案是换黄绿(约 571nm、Vf≈2.1V)0603 保持 R9=1k 得到 1.2 mA,但要重验极性和 CPL 角度。

---

## B · 下单前必须外部确认(本地无法验证,按风险排序)

- **B1 CPL 贴片角度** —— 93 颗元件方向。重点 D5(CPL 写 270°)、J2、U4、U5、X1。
  贴反是整批报废,所有条目里赔钱最快的一条。
- **B2 封装焊盘图 vs 数据手册** —— 我核的是「铜箔 == 清单」,清单本身的引脚映射要拿手册对。
  风险集中在 U1/U3(AP6320x 的 FB/VIN/EN 顺序)、U5(HVQFN-40)、U6、Q1(AO4407A 的 S/G/D)。
- **B3 LCSC 当日库存** —— 21 个料号缺口已关闭,但确认 ≠ 锁库存。重点 RF 的 C0G 电容和 U5。
- **B4 嘉立创 Gerber 查看器目视** —— 层序、板框、长槽、NFC/U4 禁铜、U5 九宫格钢网。

---

## C · DFM 实测(已对嘉立创四层 1oz 官方能力表核实)

| 参数 | 本板 | 嘉立创 | 结论 |
|---|---|---|---|
| 最小线宽/间距 | 0.20 / 0.10 mm | 0.09 / 0.09 mm | 宽裕通过 |
| 最小钻孔 | 0.20 mm | 0.15 mm | 通过 |
| 过孔环宽 ×218 | 0.150 mm | 推荐 0.20 / 下限 0.15 | **零余量** |
| 过孔环宽 ×13 | 0.200 mm | 推荐 0.20 | 通过 |
| NPTH 零环宽 ×6 | 0 mm | 不适用 | 正确 |

**C1** 231 个过孔里 218 个是 `0.5 mm 焊盘 / 0.2 mm 钻孔` = 0.150 mm 环宽,正好卡在绝对下限,
对钻偏没有余量。首批 5 片原型可以照做;转小批量把焊盘提到 0.6 mm(钻孔不变)得到 0.20 mm,
但要重新布线并重跑间距 DRC,属 Rev B。

---

## D · 仿真结论(ngspice 实跑)

天线模型从 `NFC_Antenna_40x40_4T` 封装真实几何量出(线宽 0.4 mm、导体总长 586.2 mm、4 匝),
匹配值直接取自 `design_data.py`。

**D1 NFC 匹配:谐振 13.601 MHz,偏 +0.30%**

```
天线模型   L = 1.491 µH   R_ac ≈ 1.8 Ω   空载 Q ≈ 71
谐振       13.601 MHz     目标 13.560     偏差 +0.30 %
-3 dB 带宽 0.762 MHz      负载 Q 17.8
```

**D2 调谐档位覆盖得住电感不确定度** —— L 是算出来的不是量出来的,所以这条比谐振点更有用:

| L 误差 | 谐振 | 修正手段 | 能拉回 |
|---|---|---|---|
| −20% | 15.02 MHz | C29/C30 68p→82p | −3.97% |
| −10% | 14.26 MHz | C31/C32 100p→120p | −4.37% |
| +10% | 13.02 MHz | C29/C30 68p→56p | +4.46% |
| +20% | 12.50 MHz | C31/C32 100p→82p | +4.99% |

电感每差 10% 谐振跑 4–5%,换一档电容也正好拉回 4–5%。首板有救。

**D3 直流工作点全部对上**

```
BAT_ADC   2.272 V @ VBAT 12.6 V   PINMAP 换算式 ×5.545 → 12.6,在 ADC 量程内
SERVO_FB  0.7682 V                TPS565201 基准 0.768 V → Vout 恰好 6.00 V
PWM_OUT   3.229 V                 R30/R31 分压
CC1/CC2   1.683 V @ 330 µA        5.1k 下拉,Type-C sink 角色正确
```

---

## E · 工程质量问题(不挡这次下单)

- **E1 工程没有根原理图** —— `kicad-cli sch erc` 直接 `Failed to load schematic`。
  不存在根 `.kicad_sch`,`.kicad_pro` 里 `sheets` 为 `null`。双击打开 Eeschema 是空白。
  文档说的「`.kicad_sch` 是权威原理图」不成立;所谓「bus error」在官方 10.0.6 上不是 bus error,
  是根本没有可加载的工程。
- **E2 5 页原理图不承载网表,819 条 ERC** —— 416 off-grid、232 符号/封装链接、60 pin_to_pin、
  **27 pin_not_connected(error)**、24 碎线头、**6 标签悬空(error)**、1 处 GND 与 USB_5V 同点。
  例:U4 pin8 EN、pin12 IRQ、pin13 VEN、U5 pin21 TX1、AE1 两脚,清单里都接了,图上是断的。
  **后果:Rev A 电气正确性只靠 `design_data.py` 一份兜底,没有第二道独立校验。**
- **E3 In1.Cu 199 个悬空铜线头** —— ~~`track_dangling` 被设成 `ignore`;地平面是 7176 段走线不是敷铜。
  残桩就在 NFC 天线正下方那层。连通性没问题(0 unconnected 已独立确认)。~~
  **已修复**:In1.Cu 的 382 段接地走线换成真正的敷铜,B.Cu 也补了一层;
  `track_dangling` 已从 `ignore` 改回 `error`,重跑 0 违规。见 J 节。
- **E4 仓库只有压缩包,构建链跑不起来**
  - git 跟踪 40 MB 归档,源文件未版本化;review 包嵌在 tar 里,同内容存约 3 份
  - `tmp/easyeda/` 未打包 —— 偏偏是修 E1/E2 要用的 `generate_schematics.py` 跑不起来
  - `TerminalBlock_bornier-2_P5.08mm` 在 KiCad 10 官方库已移除(12 个 stock 封装唯一一个)
  - `.tools/py`、`.tools/kicad10-full-root`、`.tools/kicad-root` 不在包里 — 已由本次钩子 shim
  - 无 `requirements.txt`,6 个三方依赖靠猜 — 已由钩子声明
  - 无 CI — 本次验证链已证明可行
- **E5 17 个封装 component type 为 Unspecified** —— 所以 `footprint_type_mismatch` 也进了忽略清单。

---

## F · Rev B 电路改进(不挡这次下单)

| 编号 | 项目 | 现状 | 建议 |
|---|---|---|---|
| F1 | USB D+/D− 串阻 | R4/R5 = 22 Ω | 改 0 Ω,C6 原生 USB 的 PHY 内部已含串阻 |
| F2 | GPIO8 悬空 | 模块 pin 22 未接 | strapping 脚,加 10k 上拉位(可 DNP) |
| F3 | 舵机 PWM 电平 | 3.229 V | DS3115 是 5–6 V 舵机,列为首板实测项 |
| F4 | RF 负载 Q | Q=17.8,BW 0.762 MHz | ±847.5 kHz 边带在 −3dB 带外;读距不佳时加大 R18/R19 降 Q |
| F5 | DNP 调谐位方向 | C33–C36 并联 | 只能加容→只能往下调频;电感偏低须换 C29/C30 本体 |
| F6 | 晶振负载电容 | 10 pF 配 10 pF | 名义 CL 约 8 pF 偏低,首板实测频偏后定值 |
| F7 | 电池分压静态电流 | ≈10.3 µA 常耗 | 待机敏感可加 MOSFET 开关 |
| F8 | D1 TVS 类型 | SMBJ15A 单向 | 反接时正向导通靠 F1 熔断,确认 F1 分断能力 |

---

## G · 已独立验证无误(不必重复排查)

PCB DRC 0/0 · 网表 309 焊盘 0 不符 · Gerber 11/11 逐字节 · PINMAP 三方一致 ·
Q1 防反接方向正确 · USB-C CC 与 USBLC6 引脚正确 · TPS565201 分压恰好 6.00 V ·
BOM 电阻编码无错配 · U4 引脚归类无遗漏 · Gerber X2 层序 L1–L4 正确

---

## H · 撤回的两条误报

- **H1** R18/R19 阻值没问题。曾疑 `0603WAF270KT5E` 是 27 Ω,查 LCSC C22946 确实是 **2.7 Ω ±1%**;
  UNI-ROYAL 用字母倍率(K=×0.01、J=×0.1),整份 BOM 自洽。
- **H2** 0.1 mm 间距不会跳工艺档。曾按「四层默认 5 mil」判偏紧,查官方能力表四层 1oz 标准就是
  **0.09/0.09 mm**,本板宽裕通过。

---

## I · 建议执行顺序

1. **改 R9:1 kΩ → 100 Ω** —— 唯一真正挡住下单的条目,成本最低。改两行、重跑发布链、
   用 `kicad-verify` 确认 Gerber 逐字节没变。
2. **核对 5 颗关键 IC 封装焊盘图**(U1/U3/U5/U6/Q1)—— 「板子回来能不能用」里我还没验过的最大一块。
3. **嘉立创预览器双确认** —— Gerber 与 PCBA 贴片角度。必须在他们界面做,贴反是整批报废。
4. **下单当天复查 LCSC 库存** —— 重点 RF 的 C0G 电容和 U5。
5. **下单后:修根原理图与 ERC** —— 目标是三方网表对拍。板子在路上时做最划算。
6. **之后:地平面改敷铜、仓库版本化、接 CI**。

---

## J · 本次已完成并推送

- `.claude/hooks/session-start.sh` —— 装 KiCad 10.0.6(PPA 签名 key 内嵌)、6 个未声明依赖、
  解包工程,补上两个便携 KiCad 路径 shim。幂等,非 web 会话静默退出。
- `.claude/skills/kicad-verify/` —— 三步独立验证,5 秒跑完,退出码可进 CI。
  `--strict` 打开被 ignore 的规则,区分「问题修好了」和「规则被关了」。
  反向测试:改一个网络、改一个 Gerber 坐标都能抓到。
- `INDEPENDENT_REVIEW_KICAD10.md` —— 首轮独立复核报告。
- **接地平面改敷铜(E3 / P3)** —— `tools/pour_ground_planes.py`:删掉 In1.Cu 的 382 段
  接地走线,In1.Cu 与 B.Cu 各铺一块真正的 GND zone,NFC 线圈和 ESP32 板边天线下方
  四层全覆盖禁铜。接地铜覆盖率 In1.Cu 42.6% → 77.2%,B.Cu 0.3% → 73.9%;三颗稳压器
  6 mm 半径内的四层接地铜合计 U1 64.1 → 206.0、U3 64.4 → 211.6、U6 65.0 → 210.7 mm²
  (各 ×3.2,满值 452.4)。`track_dangling` 199 → 0,并已从 `ignore` 改回
  `error` —— 工程侧现在**没有任何一条 DRC 规则被关掉**。
  - zone 以**未填充**状态入库,所以 `kiutils` 和工程里全部审计脚本照常解析;填充交给
    `kicad-cli`(`drc --refill-zones` / `export gerbers --check-zones`),制造图仍由一条
    确定的命令产出。
  - 敷铜会被走线切碎;7 块与地断开的碎铜里 2 块够大,自动补了缝合过孔(0.3/0.6 mm,
    PTH 孔数 240 → 242),其余 5 块共 4.91 mm² 由 island removal 删除,**成品不留浮铜**。
  - `audit_board.py` 增加一条检查:敷铜轮廓减去禁铜区之后不得进入两个天线禁区
    —— 反向测试(把禁铜区改小)会报 953 mm² 越界。
  - `audit_connectivity.py` 现在把敷铜算作铜;填充层面的连通性由 `drc --refill-zones`
    的 0 unconnected 背书。反向测试(删掉 105 个接地过孔)仍会报 GND 断成 71 块。
- **U4 位号移回板内** —— 上一版 U4 的位号落在 (99, −1.0),在板框外面。修 `design_data.py`
  → 项目封装库那一步让文件里出现 `(version 20240108)`,KiCad 从此把 U4 的
  `(property "Reference")` 当成真字段解析,位号会真的印出来,于是把它从 (0, −9.5) 挪到
  (0, +9.5),落在 (99, 18.0),U4 courtyard 下方的空白带里。

---

通过 A–D 不等于可以量产,只说明板子内部自洽、文件与板子一致。
首板的 NFC 天线 VNA 调谐、HomeKey 穿门距离、待机功耗、舵机堵转与机构验证仍然必须做。
